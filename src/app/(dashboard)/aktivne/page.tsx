import {
  ActiveToursContainer,
  ActiveToursHeader,
  ActiveToursList,
} from "@/components/active-tours/index";

export default function AktivnePage() {
  return (
    <ActiveToursContainer>
      <ActiveToursHeader />
      <ActiveToursList />
    </ActiveToursContainer>
  );
}
